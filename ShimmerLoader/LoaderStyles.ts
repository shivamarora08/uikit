import { StyleSheet } from 'react-native';

const loaderStyles = StyleSheet.create({
    cardContainerStyle: {
        minHeight: 80,
        justifyContent: 'center',
        marginBottom: 8,
    },
    mediaSeparator: {
        marginTop: 16,
    },
    media: {
        minHeight: 55,
    },
});

export { loaderStyles };
