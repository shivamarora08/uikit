import { StyleSheet } from 'react-native';

const frameListStyles = StyleSheet.create({
    item: {
        // marginRight: 4,
        marginBottom: 16,
    },
    columnWrapperStyle: {
        flexWrap: 'wrap',
        flex: 1,
        alignItems: 'center',
    },
});

export { frameListStyles };
